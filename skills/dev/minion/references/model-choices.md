# Model Choices

- Preserve a model the user explicitly names.
- Otherwise inherit the current host's model and reasoning defaults when appropriate.
- Resolve exact model IDs and effort settings from the current runner.
- Report an unavailable requested model instead of silently substituting another.
- Use an independent provider when the task calls for another perspective.
- Use the smallest useful number of agents and verify the integrated result.
